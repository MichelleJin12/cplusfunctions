sort함수는 배열이나 벡터나 구조체 같은 것들을 정렬해주는 함수임.
근데 나는 아직 실력이 안돼서 배열이랑 벡터 정도만 정렬함...
처음에 <algorithm>을 포함해줘야함...
그리고 std::sort이렇게 사용하면 된다.
sort말고도 stable_sort도 있다는데 이거는 아직 쓰기 귀찮음...sort먼저 공부하고 써야지

배열에서 sort를 쓰면 sort(배열의포인터, 소팅하고싶은데까지 배열포인터+N);해주면 됨
std::sort(arr, arr+100);

벡터를 쓰면 std::sort(a.begin(), a.end()); 이렇게 쓰면 된다구함.....a가 벡터임


함수 맨날 까먹어서 내가 구현하다가 시간 다보내고 뻘짓한다...근데 난 내가 구현하는게 재밌던데 그냥 라이브러리 쓰는것보다
